# AI Architecture

Describe the AI layer:

- Module layout (e.g. /lib/ai-recruiting, /lib/ai-evaluation)
- Data sources (schema tables, derived stats)
- Output contracts (score, confidence, explanation)
- Where AI outputs surface in the UI (roster, recruiting, profile, etc.)
